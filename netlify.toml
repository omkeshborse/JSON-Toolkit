# Netlify configuration for JSON Toolkit

[build]
  command = "npm run build"
  publish = "dist"

# Route all requests to index.html with 200 rewrite for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
